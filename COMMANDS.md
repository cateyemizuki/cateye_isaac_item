# 以撒道具图鉴 — 指令与触发词说明

## 指令一览

| 指令 | 备用写法 | 功能 | 权限 |
|------|---------|------|------|
| `/以撒 <关键词>` | `以撒 <关键词>`、`/isaac <关键词>`、`/以撒道具 <关键词>`、`/以撒图鉴 <关键词>` | 查询条目：中文名、英文名、编号、标签、效果关键词都可以；唯一或精确命中直接给详情，否则给候选列表 | 所有人 |
| `/以撒详情 <关键词>` | `/以撒详情<关键词>` | 只输出完整效果、套装与协同 | 所有人 |
| `/以撒协同 <关键词>` | `/以撒协同<关键词>` | 输出该道具的全部协同条目 | 所有人 |
| `/以撒编号 <编号>` | `/以撒编号<编号>` | 按图鉴编号查询（等价于 `/以撒 <数字>`） | 所有人 |
| `/以撒随机 [分类]` | `/以撒随机` | 随机抽一条；分类可选 `道具` / `饰品` / `卡牌` / `药丸` | 所有人 |
| `/以撒` | `/以撒帮助`、`/以撒help`、`/以撒菜单` | 帮助信息 + 数据来源与规模 | 所有人 |

指令前缀 `/` 可省略；`/以撒` 与 `/isaac` 等价。命令命中后会拦截后续处理，不会同时触发 LLM 回复。

## 实际输出示例

### `/以撒 硫磺火`（协同超过 5 条 → 正文 + 合并转发）

第 1 条（普通文本）：

```
【硫磺火】 Brimstone
品质 4 极品 · 道具 · 编号 118
标签：攻击性 / 恶魔 / 可召唤
效果：
角色的攻击方式改为蓄力发射 1 个鲜血激光柱。在该攻击方式下，射速修正 × 1/3 蓄力时间与射速成反比。
鲜血激光柱具有无限射程，可以穿透敌人和障碍物。……
协同（共 71 条）：全部协同见下方合并转发。
```

第 2 条（合并转发，含全部 71 条协同）：

```
【硫磺火】协同共 71 条：
· 我的镜像：角色移动时，鲜血激光柱会弯曲。
· 胎儿博士：角色在蓄力的同时会发射炸弹。
· 科技：鲜血激光柱的视觉效果改为被科技的激光围绕，伤害修正 × 150 %。
……（共 71 行）
```

### `/以撒 影之书`（协同 ≤ 5 条 → 直接内联，不转发）

```
【影之书】 Book of Shadows
品质 3 好 · 道具 · 编号 58
标签：攻击性 / 书
效果：
使用后：角色在当前房间中获得一个免疫所有伤害的护盾，持续 10 秒。多次使用后，影之书的护盾持续时间会叠加。
协同（共 5 条）：
· 剃刀片：护盾期间使用剃刀片仍然会受伤。
· 以撒的心脏：护盾持续期间心脏不会受到伤害，即使动画效果上心脏没有受到保护。
· 美德之书：生成的魂火跟班在护盾持续期间不会受到伤害但依然能对敌方造成伤害。
· 所罗门魔典：生成的魂火跟班在护盾持续期间不会受到伤害但依然能对敌方造成伤害。
· 车载电池：护盾持续时间变为20秒。
```

### `/以撒 洋葱`（多个匹配 → 候选列表）

```
「洋葱」找到 2 条：
1. 【烂洋葱】 Dead Onion（品质 2 不错 · 道具 · 编号 336 · 标签 攻击性/可召唤/死亡 · 射程 -1.50 上抛速度 -0.50 弹速 -0.40…）
2. 【悲伤洋葱】 The Sad Onion（品质 3 好 · 道具 · 编号 1 · 标签 攻击性/可召唤/射速上升 · 射速 +0.70）
用 /以撒详情 <名称> 查看完整效果，/以撒随机 抽一个道具。
```

### `/以撒 射速`（名称匹配不到 → 标签检索）

```
「射速」找到 5 条：
1. 【射速下降】（药丸 · 编号 3015）
2. 【射速上升】（药丸 · 编号 3016）
3. 【光环】 The Halo（品质 3 好 · 道具 · 编号 101 · 标签 攻击性/可召唤/射速上升 · …）
...
```

### `/以撒 飞行`（标签也匹配不到 → 效果正文检索）

```
「飞行」找到 5 条：
1. 【剖腹产】 C Section（品质 4 极品 · 道具 · 编号 678 · …）
2. 【巨人独眼】 Polyphemus（品质 4 极品 · 道具 · 编号 169 · …）
...
```

### `/以撒 sad onin`（拼错 → 模糊匹配）

```
「sad onin」找到 4 条：
1. 【悲伤洋葱】 The Sad Onion（品质 3 好 · 道具 · 编号 1 · 射速 +0.70）
...
```

### `/以撒随机 饰品`（数据源只有名称的类型会如实说明）

```
【参孙的发髻】
饰品 · 编号 1058
效果：数据源只收录了该饰品的名称（上游只对「道具」抓到了完整效果文本）。
```

### 未命中

```
没找到和「不存在的道具xyz」相关的条目。
可以试试中文名、英文名、编号，或者 /以撒随机 抽一个。
```

### `/以撒详情 297`（wiki 里是表格的效果 → 已补录，逐行渲染）

```
【潘多拉魔盒】 Pandora's Box
品质 2 不错 · 道具 · 编号 297
标签：攻击性
效果：
使用后：如果角色拥有奇怪的钥匙，则随机生成 6 个随机道具池的道具，并移除奇怪的钥匙；否则根据当前楼层触发以下效果：
根据当前楼层触发以下效果（表：普通/困难模式楼层 ｜ 贪婪模式 ｜ 效果）：
· 地下室1 / 地窖1 / 燃烧地下室1 / 下水道1 / 污水井1 ｜ 地下室 ｜ 生成 2 个魂心
· 地下室2 / 地窖2 / 燃烧地下室2 / 下水道2 / 污水井2 ｜ 洞穴 ｜ 生成 2 个炸弹和 2 个钥匙
……（共 16 行）
补充：
· 「迷宫诅咒！」的楼层以当前章节的第一层计算。
· 由潘多拉魔盒生成的硬币不会变成其他种类的硬币。
协同（共 2 条）：
· 虚空：被吞噬后，只在下次使用虚空时触发一次潘多拉魔盒的效果。
· 车载电池：无额外效应。
```

### 表格类条目

13 条 wiki 表格类条目（75 / 263 / 286 / 297 / 303 / 348 / 373 / 515 / 619 / 654 / 658 / 664 / 683）已全部补录，
查询时会直接逐行渲染表格或补充说明。

若将来出现**未补录**的截断条目，会退化为附上灰机 wiki 页面链接：

```
……效果：
使用后触发以下效果：
（此条在 wiki 中以表格/列表给出，本地数据源未收录，详见 https://isaac.huijiwiki.com/wiki/<条目名>）
```

可用 `display.show_wiki_link = false` 关掉这个兜底提示。

## 协同展示规则

| 情况 | 行为 |
|---|---|
| 协同条数 ≤ `display.max_synergies`（默认 5） | 全部内联在详情里，不发转发 |
| 协同条数 > `display.max_synergies` | 正文只留一行「协同（共 N 条）：全部协同见下方合并转发。」，随后**用一条合并转发发送全部协同**（不折叠、不丢条） |
| `display.max_synergies = 0` | 全部内联展示（不因协同触发转发；正文过长时仍按 `forward_threshold` 走合并转发） |
| 合并转发不可用（适配器不支持或调用报错） | 自动回退为普通文本发送，内容不丢 |
| LLM 工具路径 | 工具发不了合并转发，改用 `display.max_synergies_in_tool`（默认 20）截断，并注明「共 N 条，此处列出前 M 条」 |

## 检索优先级

`编号` → `名称精确` → `名称前缀` → `名称包含` → `英文名` → `标签` → `效果正文` → `协同正文` → `模糊近似`（相似度阈值默认 0.55）。

前一级已经凑够 `search.max_results` 条时不再往下退，避免结果被稀释；`search.search_by_tag` /
`search_by_effect` / `search_by_synergy` 可以分别关掉后三级。

**协同检索**的典型用法：想找「和某个道具/机制有协同的道具」时直接搜那个关键词，例如
`/以撒 不再叠加`（→ 变异蜘蛛）、`/以撒 被吞噬后`（→ 生死逆转、神秘礼物、熔炉等）。
反过来查「某个道具和谁有协同」用 `/以撒协同 <道具名>`。

## LLM 工具 `isaac_item_lookup`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `query` | string | 是 | 中文名、英文名、图鉴编号或效果关键词 |
| `detail` | boolean | 否 | `true` 返回完整效果与协同；默认 `false`，多个匹配时返回候选列表 |

返回 `content` 为已排版的文本，模型可直接引用；工具本身不发送消息。
