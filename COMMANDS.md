# 以撒道具图鉴 / 存档解析 — 指令与触发词说明

## 指令一览

| 指令 | 备用写法 | 功能 | 权限 |
|------|---------|------|------|
| `/以撒 <关键词>` | `以撒 <关键词>`、`/isaac <关键词>`、`/以撒道具 <关键词>`、`/以撒图鉴 <关键词>` | 查询条目：中文名、英文名、编号、标签、效果关键词都可以；唯一或精确命中直接给详情，否则给候选列表 | 所有人 |
| `/以撒详情 <关键词>` | `/以撒详情<关键词>` | 只输出完整效果、套装与协同 | 所有人 |
| `/以撒协同 <关键词>` | `/以撒协同<关键词>` | 输出该道具的全部协同条目 | 所有人 |
| `/以撒编号 <编号>` | `/以撒编号<编号>` | 按图鉴编号查询（等价于 `/以撒 <数字>`） | 所有人 |
| `/以撒随机 [分类]` | `/以撒随机` | 随机抽一条；分类可选 `道具` / `饰品` / `卡牌` / `药丸` | 所有人 |
| `/以撒` | `/以撒帮助`、`/以撒help`、`/以撒菜单` | 帮助信息 + 数据来源与规模（含存档指令清单） | 所有人 |
| `/以撒存档` | `/以撒进度`、`/isaac存档` | 查看解析图 + 合并转发的详细进度；还没绑定时给出「去哪找存档」指引 | 所有人 |
| `/以撒存档绑定` | `/以撒存档上传`、`/以撒存档更新` | 引用（回复）群文件里的 `.dat` 后绑定 / 更新该存档位的存档 | 所有人 |
| `/以撒存档<1~3>` | `/以撒存档 2` | 查看指定存档位的解析结果 | 所有人 |
| `/以撒存档详情` | `/以撒存档文本` | 只要文字详情（不发解析图） | 所有人 |
| `/以撒存档列表` | `/以撒存档状态` | 查看本地已缓存的存档（存档位、文件名、版本、绑定时间） | 所有人 |
| `/以撒存档清除` | `/以撒存档删除`、`/以撒存档解绑` | 删除 bot 本地缓存的该用户全部存档（不影响用户自己的存档文件） | 所有人 |
| `/以撒存档帮助` | `/以撒存档说明` | 存档位置与上传方式指引 | 所有人 |

指令前缀 `/` 可省略；`/以撒` 与 `/isaac` 等价。命令命中后会拦截后续处理，不会同时触发 LLM 回复。
`/以撒存档` 系列与 `/以撒 <道具>` 的匹配互不抢占（`/以撒存档` 不会被当成图鉴关键词）。

### 什么时候用普通文本、什么时候用合并转发

一条回复**字数超过 `forward_threshold`（默认 400）**或**行数超过 `forward_max_lines`（默认 12）**
时，插件改用**合并转发**发送，聊天窗里只占一条（点开是聊天记录）——避免刷屏：

| 场景 | 实测字数 / 行数 | 发送方式 |
|---|---|---|
| `/以撒`、`/以撒帮助`（帮助 + 数据来源） | 848 字 / 23 行 | **合并转发** |
| `/以撒 硫磺` 这类候选列表 | 629 字 / 7 行 | **合并转发** |
| `/以撒 硫磺火`（正文 505 字 + 协同 3124 字） | — | **一条合并转发**（正文与协同合成同一条聊天记录，不再「一条正文 + 一条转发」） |
| `/以撒 大胃王` 这类长效果条目 | 708 字 / 23 行 | **合并转发** |
| `/以撒 血袋`、`/以撒随机` 这类短回复 | 55~142 字 | 普通文本 |
| `/以撒存档`、`/以撒存档详情` | 详情 2761 字 | 解析图/摘要 + **合并转发**详情 |
| `/以撒存档列表`、错误提示 | ≤ 165 字 | 普通文本 |

两个阈值都可以在 WebUI 的「显示」节调整（填 0 关闭对应判断；都为 0 时始终用普通文本）。
合并转发不可用（适配器不支持）时逐段回退为普通文本、超长按 1200 字切块，不会丢内容。


## 实际输出示例

### `/以撒 硫磺火`（协同超过 5 条 → 正文与协同合成一条合并转发）

发送一条合并转发（内含 2 段：先正文，再全部 71 条协同）：

```
【硫磺火】 Brimstone
品质 4 极品 · 道具 · 编号 118
标签：攻击性 / 恶魔 / 可召唤
效果：
角色的攻击方式改为蓄力发射 1 个鲜血激光柱。在该攻击方式下，射速修正 × 1/3 蓄力时间与射速成反比。
鲜血激光柱具有无限射程，可以穿透敌人和障碍物。……
协同（共 71 条）：全部协同见本条合并转发的下一段。
```

```
【硫磺火】协同共 71 条：
· 我的镜像：角色移动时，鲜血激光柱会弯曲。
· 胎儿博士：角色在蓄力的同时会发射炸弹。
· 科技：鲜血激光柱的视觉效果改为被科技的激光围绕，伤害修正 × 150 %。
……（共 71 行）
```

### `/以撒 影之书`（协同 ≤ 5 条 → 直接内联，不转发）

```
【影之书】 Book of Shadows
品质 3 好 · 道具 · 编号 58
标签：攻击性 / 书
效果：
使用后：角色在当前房间中获得一个免疫所有伤害的护盾，持续 10 秒。多次使用后，影之书的护盾持续时间会叠加。
协同（共 5 条）：
· 剃刀片：护盾期间使用剃刀片仍然会受伤。
· 以撒的心脏：护盾持续期间心脏不会受到伤害，即使动画效果上心脏没有受到保护。
· 美德之书：生成的魂火跟班在护盾持续期间不会受到伤害但依然能对敌方造成伤害。
· 所罗门魔典：生成的魂火跟班在护盾持续期间不会受到伤害但依然能对敌方造成伤害。
· 车载电池：护盾持续时间变为20秒。
```

### `/以撒 洋葱`（多个匹配 → 候选列表）

```
「洋葱」找到 2 条：
1. 【烂洋葱】 Dead Onion（品质 2 不错 · 道具 · 编号 336 · 标签 攻击性/可召唤/死亡 · 射程 -1.50 上抛速度 -0.50 弹速 -0.40…）
2. 【悲伤洋葱】 The Sad Onion（品质 3 好 · 道具 · 编号 1 · 标签 攻击性/可召唤/射速上升 · 射速 +0.70）
用 /以撒详情 <名称> 查看完整效果，/以撒随机 抽一个道具。
```

### `/以撒 射速`（名称匹配不到 → 标签检索）

```
「射速」找到 5 条：
1. 【射速下降】（药丸 · 编号 3015）
2. 【射速上升】（药丸 · 编号 3016）
3. 【光环】 The Halo（品质 3 好 · 道具 · 编号 101 · 标签 攻击性/可召唤/射速上升 · …）
...
```

### `/以撒 飞行`（标签也匹配不到 → 效果正文检索）

```
「飞行」找到 5 条：
1. 【剖腹产】 C Section（品质 4 极品 · 道具 · 编号 678 · …）
2. 【巨人独眼】 Polyphemus（品质 4 极品 · 道具 · 编号 169 · …）
...
```

### `/以撒 sad onin`（拼错 → 模糊匹配）

```
「sad onin」找到 4 条：
1. 【悲伤洋葱】 The Sad Onion（品质 3 好 · 道具 · 编号 1 · 射速 +0.70）
...
```

### `/以撒随机 饰品`（数据源只有名称的类型会如实说明）

```
【参孙的发髻】
饰品 · 编号 1058
效果：数据源只收录了该饰品的名称（上游只对「道具」抓到了完整效果文本）。
```

### 未命中

```
没找到和「不存在的道具xyz」相关的条目。
可以试试中文名、英文名、编号，或者 /以撒随机 抽一个。
```

### `/以撒详情 297`（wiki 里是表格的效果 → 已补录，逐行渲染）

```
【潘多拉魔盒】 Pandora's Box
品质 2 不错 · 道具 · 编号 297
标签：攻击性
效果：
使用后：如果角色拥有奇怪的钥匙，则随机生成 6 个随机道具池的道具，并移除奇怪的钥匙；否则根据当前楼层触发以下效果：
根据当前楼层触发以下效果（表：普通/困难模式楼层 ｜ 贪婪模式 ｜ 效果）：
· 地下室1 / 地窖1 / 燃烧地下室1 / 下水道1 / 污水井1 ｜ 地下室 ｜ 生成 2 个魂心
· 地下室2 / 地窖2 / 燃烧地下室2 / 下水道2 / 污水井2 ｜ 洞穴 ｜ 生成 2 个炸弹和 2 个钥匙
……（共 16 行）
补充：
· 「迷宫诅咒！」的楼层以当前章节的第一层计算。
· 由潘多拉魔盒生成的硬币不会变成其他种类的硬币。
协同（共 2 条）：
· 虚空：被吞噬后，只在下次使用虚空时触发一次潘多拉魔盒的效果。
· 车载电池：无额外效应。
```

### 表格类条目

13 条 wiki 表格类条目（75 / 263 / 286 / 297 / 303 / 348 / 373 / 515 / 619 / 654 / 658 / 664 / 683）已全部补录，
查询时会直接逐行渲染表格或补充说明。

若将来出现**未补录**的截断条目，会退化为附上灰机 wiki 页面链接：

```
……效果：
使用后触发以下效果：
（此条在 wiki 中以表格/列表给出，本地数据源未收录，详见 https://isaac.huijiwiki.com/wiki/<条目名>）
```

可用 `display.show_wiki_link = false` 关掉这个兜底提示。

## 存档解析实际输出示例

### `/以撒存档`（还没绑定过 → 存档位置指引，走合并转发）

第 1 条（普通文本）：

```
还不认识你的存档，先把存档文件发给 bot 吧。
```

第 2 条（合并转发，节选）：

```
【一、找到存档文件】
存档在游戏目录之外，按版本在这些位置（文件名里的 1/2/3 是存档位）：
· 忏悔+：文档\My Games\Binding of Isaac Repentance+\rep+persistentgamedata1.dat
· 忏悔 ：文档\My Games\Binding of Isaac Repentance\rep_persistentgamedata1.dat
· 胎衣+：文档\My Games\Binding of Isaac Afterbirth+\abp_persistentgamedata1.dat
· 开了 Steam 云存档的话，也可能在：Steam\userdata\<你的 SteamID>\250900\remote\
提示：要的是 persistentgamedata，不是 gamestate（那是「中途退出的单局」，统计不了进度）。

【二、发给我】
1. 把 .dat 文件传到群文件（手机 QQ 可用「文件 → 上传到群文件」）；
2. 在群里引用（回复）那个文件消息，发送 /以撒存档绑定；
3. 也可以直接把文件拖进聊天窗口发送，再说一句 /以撒存档绑定。

【三、其它用法】
/以撒存档          查看解析结果（解析图 + 详细进度）
/以撒存档绑定      绑定 / 更新你引用的存档
/以撒存档2         查看存档位 2（如果你上传了多个存档位）
/以撒存档清除      删除 bot 本地缓存的存档

存档只会保存在 bot 本地用于解析，解析是只读的，不会修改你的存档。
```

### 引用群文件 + `/以撒存档绑定`（绑定成功 → 解析图 + 详情）

第 1 条（普通文本）：

```
存档已收好：rep+persistentgamedata1.dat（15.7 KB，忏悔+（Repentance+））
已按你的 QQ 号缓存在 bot 本地，存档位 1；解析是只读的，不会改动原文件。
```

第 2 条（图片，即解析图）：进度条 + 统计卡 + 未发现道具标签，例如

```
以撒的结合 · 存档解析          [忏悔+（Repentance+）] [存档位 1]
CatEye · QQ 1434817453 · 存档位 1 · rep+persistentgamedata1.dat · 绑定于 2026-09-16 20:31

进度总览   成就 605/641 94.4% ｜ 道具 703/732 96.0% ｜ BOSS 记录 97/104 93.3%
           小 BOSS 记录 7/7 100% ｜ 挑战 16/45 35.6% ｜ 特殊种子 0/79 0.0%
全局统计   226 妈妈击杀 ｜ 438 死亡次数 ｜ 58 店主击杀 ｜ 59,304 碎石数 ｜ …
还没拿到的道具（29 件）  59 彼列之书 ｜ 189 超级食肉男孩死忠粉 ｜ 235 未收录 ｜ 237 死神之触 ｜ …
```

第 3 条（合并转发，完整详情）：

```
【存档解析】rep+persistentgamedata1.dat
忏悔+（Repentance+） · 存档位 1 · 15.7 KB · 整段对齐（尾部保留 8 字节）

一、进度总览
· 成就 605/641（94.4%）
· 道具 703/732（96.0%）
· BOSS 记录 97/104（93.3%）
· 小 BOSS 记录 7/7（100.0%）
· 挑战 16/45（35.6%）
· 特殊种子 0/79（0.0%）

二、全局统计
· 妈妈击杀：226
· 死亡次数：438
· 店主击杀：58
· 碎石数：59,304
· 大便破坏：24,948
· 染色石破坏：787
· 累计捐款：1,092
· 伊甸币：223
· 最佳连胜：11
· 当前连胜：0

三、还没拿到的道具（29 件）
· 59 彼列之书
· 189 超级食肉男孩死忠粉
· 235（图鉴未收录该编号，可能是空槽位）
· 237 死神之触
· 244 科技0.5
……（其余按编号列出）
（其中 9 个编号在图鉴里没有收录；已识别中文名 20 个。）

四、未解锁的挑战（29 个）
· 7 自杀之王
· 8 好奇害死猫
· 13 豆子！
· 14 尽在卡牌中
……（按编号列出全部）

五、未解锁的成就（36 个）
（条件取自游戏本体 achievements.xml 的英文原文——游戏内没有中文版本。）
· 60 焦灼硬币：beat Challenge #13
· 63 超级食肉男孩死忠粉：Beat Challenge #14
· 69 !Platinum God!：collect all items in the game and unlock all secrets and endings minus "the soul" …
· 84 The Real Platinum God：100% of the game (unlock every item)
· 95 桦木符文：Beat Challenge #7
……（按编号列出全部，最多 detail_max_named 条；名字里 84% 是官方中文译名，其余退回游戏英文标题）

六、存档没有记录的 BOSS（7 个）
· 62 究极贪婪：游戏不把这一项写进存档 BOSS 段（图鉴显示已击杀也不置位）
· 71 究极大贪婪：游戏不把这一项写进存档 BOSS 段（图鉴显示已击杀也不置位）
· 72 胖蛆族母：图鉴里击杀次数恒为 0（该 BOSS 无法被正常击败，因此不会有记录）
· 89 游戏未分配的空槽位（当前 entities2.xml 里没有这个 BOSS 编号）
· 90 游戏未分配的空槽位（当前 entities2.xml 里没有这个 BOSS 编号）
· 98 绷带巫妖：在核对过的存档里从未出现（可能为未启用内容）
· 103 游戏未分配的空槽位（当前 entities2.xml 里没有这个 BOSS 编号）

说明：只读解析，不会修改你的存档。怪物图鉴（bestiary）段本插件只跳过、不参与统计。名称数据来源：成就名称取自游戏本体 achievements.xml + 官方简体语言包（tools/build_achievement_table.py 生成，无第三方数据）；BOSS 与小 BOSS 名称取自游戏本体语言包（isaac_entities_zh.json、isaac_minibosses_zh.json），BOSS 段 104 个槽位的下标取自游戏本体 entities2.xml 的 bossID 属性（tools/build_boss_table.py）；挑战名为灰机 wiki「挑战」页面整理（通常 CC BY-NC-SA 系，与本插件道具图鉴数据同类，代码仍为 MIT）
```

> 「存档没有记录的小 BOSS」段只在**有未遇到的七宗罪**时出现，例如：

```
六、存档没有记录的小 BOSS（3 个）
· 1 色欲
· 3 暴食
· 5 嫉妒
```

### 上传错了文件（`gamestate` / 压缩包 / 过大）

绑定时会直接说明原因，不会静默失败：

```
这是「进行中的一局」状态文件（gamestate），不是持久存档，无法用于进度统计。
请在游戏里退回主菜单后再复制存档，文件名应为 rep+persistentgamedata1.dat 这种。
```

```
这个文件不是《以撒的结合》存档（文件头不匹配）。
读到的文件头：b'PK\x03\x04\x14\x00\x00\x00'
常见原因是上传了压缩包、快捷方式或其它文件。
```

### `/以撒存档列表`

```
已缓存的存档：
· 存档位 1：rep+persistentgamedata1.dat（15.7 KB，忏悔+（Repentance+））
  绑定于 2026-09-16 20:31｜来源 群文件引用
· 存档位 2：rep+persistentgamedata2.dat（4.0 KB，忏悔+（Repentance+））
  绑定于 2026-09-15 20:48｜来源 群文件引用
发送 /以撒存档 查看解析结果，/以撒存档清除 删除缓存。
```

## 协同展示规则

| 情况 | 行为 |
|---|---|
| 协同条数 ≤ `display.max_synergies`（默认 5） | 全部内联在详情里，不发转发 |
| 协同条数 > `display.max_synergies` | 正文只留一行「协同（共 N 条）：全部协同见下方合并转发。」，随后**用一条合并转发发送全部协同**（不折叠、不丢条） |
| `display.max_synergies = 0` | 全部内联展示（不因协同触发转发；正文过长时仍按 `forward_threshold` 走合并转发） |
| 合并转发不可用（适配器不支持或调用报错） | 自动回退为普通文本发送，内容不丢 |
| LLM 工具路径 | 工具发不了合并转发，改用 `display.max_synergies_in_tool`（默认 20）截断，并注明「共 N 条，此处列出前 M 条」 |

## 检索优先级

`编号` → `名称精确` → `名称前缀` → `名称包含` → `英文名` → `标签` → `效果正文` → `协同正文` → `模糊近似`（相似度阈值默认 0.55）。

前一级已经凑够 `search.max_results` 条时不再往下退，避免结果被稀释；`search.search_by_tag` /
`search_by_effect` / `search_by_synergy` 可以分别关掉后三级。

**协同检索**的典型用法：想找「和某个道具/机制有协同的道具」时直接搜那个关键词，例如
`/以撒 不再叠加`（→ 变异蜘蛛）、`/以撒 被吞噬后`（→ 生死逆转、神秘礼物、熔炉等）。
反过来查「某个道具和谁有协同」用 `/以撒协同 <道具名>`。

## LLM 工具 `isaac_item_lookup`

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `query` | string | 是 | 中文名、英文名、图鉴编号或效果关键词 |
| `detail` | boolean | 否 | `true` 返回完整效果与协同；默认 `false`，多个匹配时返回候选列表 |

返回 `content` 为已排版的文本，模型可直接引用；工具本身不发送消息。
