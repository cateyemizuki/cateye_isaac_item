# Changelog

## [0.2.7] - 许可结构重组：整包 CC BY-NC-SA 4.0（代码与非 CC 数据另以 MIT 开源）+ 存档缓存默认 30 天

- **修掉「标 MIT、实际整包非商业」的许可误导**：0.2.6 及之前 `LICENSE` 与 `manifest.license` 写的是 MIT，
  但 `assets/` 里 4 个 wiki 来源数据文件（`isaac_items.json`、`isaac_extra.json`、`isaac_challenges_zh.json`、
  `isaac_achievements_zh.json`）带 CC BY-NC-SA 的「需署名 / 不可商用 / 相同方式共享」限制，
  README 又声明整包仅非商业使用——插件市场按 manifest 展示许可会误导用户。本轮按「整包 CC、代码 MIT」重组：
  - `LICENSE` 替换为 **CC BY-NC-SA 4.0 官方全文**；`manifest.license` 同步改为 `CC BY-NC-SA 4.0`，
    `description` 开头加「【非商业插件】」显著标注（市场列表截断也看得见）；
  - README **开头**新增「许可（请先阅读）」一节：逐文件列出受 CC BY-NC-SA 控制的 4 个 wiki 数据文件及来源、
    声明**不受 CC 控制的内容（全部 `*.py` 代码、游戏本体数据、`tools/` 脚本等）以 MIT 开源并附 MIT 全文**、
    字体 SIL OFL 1.1；底部「致谢 → 许可说明」同步改成同一口径；
  - 生成工具（`build_achievement_table.py` / `build_boss_table.py`）写入数据 `meta.license` 的字样
    从「随本插件按 MIT 发布」改为「随仓库以 MIT 提供（整包 CC BY-NC-SA 4.0）」，两份现有 JSON 同步更新；
  - `tools/check_licenses.py` 的许可证一致性断言从「`LICENSE` 与 `manifest.license` 均为 MIT」
    改为「均为 CC BY-NC-SA 4.0」，结论输出同步说明两部分受控范围。
  这也是 0.2.5「为什么不是把插件整体改成 GPL-3.0」的自然落点：wiki 数据按 CC 引入 → 整包 CC BY-NC-SA 4.0；
  代码与非 CC 数据单独以 MIT 开放，两边互不传染、也互不改变。
- **存档缓存默认 30 天**：`save.cache_ttl_days` 默认值从 `0`（永久）改为 **`30`**，到期自动清理
  （`0` 仍表示永久保留）。绑定成功的提示与 `/以撒帮助` 的存档段都会显示当前有效期
  （「缓存 30 天后自动过期，/以撒存档清除 可随时删除」），不再默认把用户存档长期留在 bot 本地；
  存量配置里已显式设置过该字段的用户值不受影响。
- 版本号与 `config_version` 同步升至 `0.2.7`（配置字段无增删，仅默认值变化）。

## [0.2.6] - 配置项汉化（WebUI 不再显示英文字段名）

- **修掉一处汉化漏项**：配置模型此前只写了 `Field(description=…)`，而 WebUI 渲染字段标题用的是
  `json_schema_extra["label"]`、副标题用 `json_schema_extra["hint"]`（`maibot_sdk/config.py::_build_field_schema`，
  `description` 只进 Schema、前端不读），`label` 缺失时**兜底成英文字段名**——
  于是「检索」「存档」等配置节的标题是中文，里面每个字段却显示成 `max_results`、`fuzzy_threshold` 这种英文键名。
- 为 **5 个配置节、37 个字段**逐一补上 `label`（短标题）+ `hint`（悬停提示）；隐藏项 `config_version` 也补了（保持隐藏）。
  `hint` 沿用 cateye 系列插件的既有约定**压到 ≤15 字**（最长 14 字），只留一句关键约束
  （默认值 / `0` 的含义 / 取值范围）；完整解释仍在 `Field(description=…)` 与 README 里，没有丢信息。
- 顺带把 5 个配置节类的 docstring 写具体（它才是 WebUI 上那一行**节描述**，原先是「检索行为配置。」这类占位）。
- **未改任何字段名、类型、默认值与校验逻辑**；`config.toml` 里仍是英文字段名，只是 WebUI 上换成中文。
  版本号与 `config_version` 同步升至 `0.2.6`（配置字段未增删，迁移只是按默认值骨架重建，用户值保留）。
- README「配置」一节同步改成「`max_results`（结果条数上限）」这种键名 + 中文标签的对照写法，便于对着 UI 找项。
- **新增自检脚本 `tools/check_config_i18n.py`**：静态 AST 扫描（不依赖 `maibot_sdk`，可用任意 Python 跑），
  若解释器装了 `maibot_sdk` 再用真实 Schema 遍历 `get_webui_config_schema()["sections"]` 复核；
  断言每节 `title` / `description` 与每个字段 `label` / `hint` 都非空、含中文、且 `hint` ≤15 字
  （`--max-hint` 可调）——5 节 37 字段全过；另跑冒烟测试确认加载、组件注册、查询与「普通聊天不误伤」均正常。

## [0.2.5] - 按 CC 许可引入中文成就表（代码仍 MIT）+ 许可审计随之更新

- **中文成就条件回来了，但没有动代码许可**：恢复 `assets/isaac_achievements_zh.json`
  （第三方中文整理，641 条）。它不是「GPL-3.0 数据」——上游仓库自己的 `THIRD_PARTY_NOTICES.md` 说明
  其**数据**来自 [英文 wiki](https://bindingofisaacrebirth.wiki.gg/wiki/Achievement)（**CC BY-SA 4.0**）与
  [以撒中文维基](https://isaac.huijiwiki.com/wiki/成就)（**CC BY-NC-SA 3.0**），上游的 GPL-3.0 只覆盖其代码。
  因此本插件按 **CC 许可**引入这份数据文件：需署名、**不可商用**、改编需相同方式共享；
  它与代码的 **MIT** 许可分开、属同一仓库内的聚合分发（mere aggregation），互不改变对方。
- **两张表叠加、字段级覆盖**：`assets/isaac_achievements.json`（游戏本体 `achievements.xml` + 官方语言包）
  提供编号、英文标题、图标名与**游戏内英文条件**；中文表只覆盖**中文名与中文条件**，
  缺项自动退回游戏本体表。新增配置 `save.use_wiki_zh_names`（默认开）——
  关掉后成就名 84% 官方中文、条件为游戏内英文原文，且不再带非商业限制。
- **新增配置 `[save] use_wiki_zh_names`**；`achievement_table_file` 的语义改为「同时接受多张表叠加」。
- **许可审计随之更新**（`tools/check_licenses.py`）：仍要求 `LICENSE` 与 `manifest.license` 都是 MIT、
  禁止 GPL 等传染性许可标记（中文表的 meta 里对「上游 GPL-3.0 只覆盖其代码」的澄清属于允许的说明）、
  逐文件登记来源，并**主动点名 4 个带非商业限制的数据文件**与「关掉中文表」的开关。
- README「致谢 / 许可说明」重写：明确「为什么不是把插件整体改成 GPL-3.0」——
  CC BY-NC-SA 的 NonCommercial 与 GPL-3.0 不兼容，改协议既救不了这份中文数据、还会让代码被传染。
- 版本号与 `config_version` 同步升至 `0.2.5`；自检 133 条断言（新增：中文表按 CC 引入、
  默认合并生效、一键关掉后退回英文条件、审计点名非商业数据）。

## [0.2.4] - 长回复一律走合并转发（帮助不再刷屏）+ 去掉 GPL-3.0 依赖、许可全面清理

### 许可：确认可整包按 MIT 开源，不再有 GPL-3.0 传染

- **移除唯一的 GPL-3.0 内容**：`assets/isaac_achievements_zh.json` 原先整表取自第三方 GPL-3.0 仓库
  （aprisyourlie/IsaacAchievementGuide）。GPL-3.0 具传染性，随 MIT 插件分发会让整个发行包背上 GPL 义务，
  该文件在 0.2.4 一度移除、0.2.5 起**按 CC 许可重新引入**（见 0.2.5 条目），同时新增 `tools/build_achievement_table.py` **从游戏本体生成**成就基线表：
  解出 `afterbirthp.a` 里的 `achievements.xml`（641 条，含编号、游戏内弹窗文案、解锁条件注释），
  中文名用官方简体语言包（`repentance_zh.a` 全 14 分类的英→中对照）反查。
- **新产物 `assets/isaac_achievements.json`**：641 条 —— 539 条（84%）拿到**官方中文名**，
  其余**退回游戏英文标题**（游戏本体没有成就名的本地化，中文只存在于 Steam 端，**不编造**）；
  284 条带**游戏内英文解锁条件**，详情里会注明「条件取自游戏本体，游戏内没有中文版本」。
  想要中文条件可用 `save.achievement_file` 指向自备的成就表（其来源与许可由使用者自行确认）。
- **新增 `tools/check_licenses.py`（许可合规审计）**：扫描产物里的 GPL / AGPL / LGPL / copyleft 标记
  （允许在 CHANGELOG、可行性评估等文档里说明历史）、核对 `LICENSE` 与 `manifest.license` 都是 MIT、
  禁止历史 GPL 文件回归、并要求每个 `assets/*.json` 都声明 `meta.source`，同时打印逐文件来源清单。
  自检里新增 9 条断言（含「GPL 表已移除」「成就表来源为游戏本体」「审计脚本通过」）。
- **许可口径写清**：代码与数据文件按 MIT 发布；灰机 wiki 来源的数据（道具图鉴、挑战名）是**数据**，
  按 CC BY-NC-SA 系使用并署名，不影响代码许可；游戏本体数据与内置字体（SIL OFL 1.1）均与 MIT 兼容。
  README「致谢 / 许可说明」与 `ISSUE_SUBMISSION.md` 的自查表同步更新。

### 发送：长回复改走合并转发

- **修掉一处刷屏**：`/以撒`、`/以撒帮助` 的帮助正文实测 **848 字 / 23 行**，但原判定只看字数
  （阈值 1000），因此它一直作为**一条普通文本**发出去，把聊天窗顶掉大半屏。
  现在改为**字数 + 行数双阈值**：正文超过 `forward_threshold`（默认 **400** 字，原 1000）
  或超过 `forward_max_lines`（新增，默认 **12** 行）就改用**合并转发**，聊天窗里只占一条。
  行数阈值专治「字数不多但行数很多」的内容（帮助、逐行渲染的长效果）。
- **正文与协同合成同一条转发**：`/以撒 硫磺火` 这类「正文较长 + 协同 71 条」的情况，
  过去是「一条普通文本 + 一条合并转发」两条消息；现在正文与协同作为**同一批合并转发的两段**发出，
  消息数减半，也不再出现那 505 字的普通文本。
- **短回复不受影响**：`/以撒 血袋`（85 字）、`/以撒随机`（55~142 字）、`/以撒存档列表`（131 字）
  等仍是普通文本——不会把所有回复都塞进聊天记录里。
- **新增配置 `[display] forward_max_lines`**（默认 12，0 = 不按行数判断）；
  `forward_threshold` 默认由 1000 调为 400（0 = 不按字数判断）。两者都为 0 时行为与旧版一致（始终普通文本）。
  合并转发不可用时仍逐段回退普通文本（超长按 1200 字切块），不丢内容。
- 实测路由（同一份数据、逐条跑过）：帮助 848/23 → 转发；候选列表 629/7 → 转发；
  `/以撒 硫磺火` 505+3124 → **一条**转发；`/以撒 大胃王` 708/23 → 转发；
  `/以撒 血袋` 85/6、`/以撒 随机` 55/3 → 普通文本。每条指令的实际字数/行数见
  [COMMANDS.md](COMMANDS.md) 的「什么时候用普通文本、什么时候用合并转发」。
- 版本号与 `config_version` 同步升至 `0.2.4`；自检 119 → **128** 条断言。

## [0.2.3] - 破译 `.a` 归档，BOSS 名表补齐（104 槽位全部有名字）

- **存档 BOSS 段的下标搞清楚了**：那 104 个字节的槽位下标 = 游戏 `entities2.xml` 里实体的
  **`bossID` 属性**（独立于实体 type 的一套编号，例 `#MONSTRO` 是 `id=20` / `bossID=1`）。
  上一轮「按 `(type, variant)` 升序」的推导已确认作废（回测 63/93、且把以撒 / 死寂 / 母亲 / 野兽排除在外），
  这次换了正确的索引依据，104 个槽位里 **100 个具名 + 4 条说明**，不再只输出编号。
- **新增 `tools/archive_reader.py`：`.a` 资源归档只读解码器（解出 `entities2.xml`）**。
  格式：`"ARCH000" + u8 版本 + u32 目录表偏移 + u16 条目数`，目录表在文件尾部，每条 20 字节；
  数据按块存 `[u32 zsize][zsize 字节]`。关键判据是**块语义**——解压端每块产出 1024 字节，
  因此 `zsize == 该块应有输出长度` 的块是**原样存储**，否则是**裸 deflate 流**
  （`zsize` 最高位只是「最后一块」标记）。按此规则 `afterbirthp.a` 10228/10228、
  `repentance.a` 4180/4180 条条目全部解码成功、长度与目录表声明一致；
  类型 1（LZW）与类型 0（加密）未实现，工具会明确报错而不是给出错误数据。
- **`tools/build_boss_table.py` 重写为「生成 + 自验证」**：由 `entities2.xml` 的 `bossID`
  生成 `assets/isaac_boss_slots.json`（逐条含 bossID / 实体 `id.variant` / 英文名 / 官方中文名 / 状态 / 说明 / 证据），
  并同步 `assets/isaac_save_names.json` 的 `bosses` 段。验证项：结构自检 + 逐份存档核验
  「未分配槽位（89 / 90 / 103）恒为 0」+ 用图鉴击杀数找出「已击杀却不置位」的槽位；
  **验证不通过就不写任何名称表**（退出码 2）。
- **名表带「说明」字段**：`assets/isaac_save_names.json` 的条目现在可以写成
  `{"name": …, "note": …}`，`note` 会作为补充说明一起显示。用于 7 个特殊槽位——
  89 / 90 / 103 是游戏未分配的空槽位、0 是胎衣时代的保留位（当前 `entities2.xml` 没有 `bossID=0`）、
  62（究极贪婪）与 71（究极大贪婪）**游戏本身不写入**（两份公开的 100% 存档里图鉴击杀 162 / 88 次却仍是 0 位）、
  72（胖蛆族母）图鉴击杀恒为 0、98（绷带巫妖）在所有存档里都没出现过。
  这样详情里写的是「存档没有记录的 BOSS」+ 逐条原因，而不是把游戏自身的行为误报成「你没打过」。
- **段名更正**：第 6 / 5 段由「BOSS 图鉴 / 小 BOSS 图鉴」改为 **「BOSS 记录 / 小 BOSS 记录」**
  ——它们是「是否已击败」的记录，真正的怪物图鉴是第 11 段。
- **怪物图鉴（第 11 段）的结构已经解出**（`[段号][段长][每组 8 字节] × 4`，
  `type = packed >> 20`、`variant = (packed >> 8) & 0xFF`），但仍**不在报告里展示**
  （少一处未经验证的数字），只在建表工具里用来核对 BOSS 槽位。
- 自检扩到 111 条断言（新增 BOSS 名表、说明字段、证据文件、正文措辞等 8 条）。
- 版本号与 `config_version` 同步升至 `0.2.3`（配置项没有变动）。

## [0.2.2] - 解析图改为纯本地绘制 + 可调宽度、去掉两项站不住的计数器、新增存档对比工具

- **解析图不再走宿主浏览器渲染**：实测宿主 `ctx.render.html2png` 经常超时失败（随后还要回退本地绘制），
  因此插件改成**只用 Pillow 本地绘制**，`_manifest.json` 里的 `render.html2png` 能力声明一并移除
  （不再需要宿主开启 `[plugin_runtime.render]`），渲染路径由「三级回退」简化为「本地绘制 → 纯文字」。
- **渲染字体随插件打包**：新增 `assets/fonts/NotoSansSC-{Regular,Bold}.ttf`
  （Noto Sans SC 子集，**SIL OFL 1.1**，许可全文 `assets/fonts/OFL.txt`，由新增的
  `tools/build_fonts.py` 生成：字符集 = GB2312 一级常用字 + 插件数据用到的全部汉字 + ASCII 与常用标点）。
  这样在没有中文字体、也没有浏览器的环境里同样能出图，两枚字体合计约 2.3 MB。
- **长宽比可调（新增 `[save] image_width`）**：卡片宽度从写死的 900px 变成配置项
  （600~1000，默认 **700**），调小时排版会真收窄——统计卡自动 4 列 → 3 列 → 2 列、
  进度条与标签同步压缩、统计卡末行不满时**居中**；因此是长宽比变瘦高，不是整体等比缩小。
  配合 `image_scale`（默认 **1.4**）控制输出像素，默认输出约 **980×1645**。
- **BOSS 名表仍未接入（该轮给出的结论已被 0.2.3 取代）**：试过用游戏侧数据推导——模组 *Enhanced Boss Bars* 的
  BOSS 实体清单（177 条 `类型.变体`）+ 存档图鉴段（本轮确定了其打包规则：
  `type = packed >> 20`、`variant = (packed >> 8) & 0xFF`，1502 条条目 type 全部合法）+
  BOSS 位图三重贪心对齐；候选解预测 93 份历史快照只有 63 份一致，且会把以撒 / 死寂 / 母亲 / 野兽
  排除在外，**判定不成立**，因此没有写入任何 BOSS 名称（该段继续只输出编号）。
  新增 `tools/build_boss_table.py`（推导 + 回测，验证不过就拒绝写入，退出码 2），
  完整结论与后续两条路线见「存档分析-可行性评估.md」附录 B。
- **全局统计从 12 项收敛到 10 项，并用 93 份每日快照确认了口径**：
  第三方偏移表把下标 `0` 标为「捐款机」、`19` 标为「捐款币」，但把
  `Documents\My Games\Binding of Isaac Repentance+\save_backups` 里
  93 份快照（2025-07 ~ 2026-09）全解析一遍后发现**这两项恒为 0**，
  同期真正的捐款机计数（下标 `20`）从 `151` 涨到 `1133`（当前 `1092`），
  且越过 900 后成就 59「蓝蜡烛（捐献 900 枚硬币给捐款机）」确实已解锁——
  所以 `0` / `19` 两项不再展示，下标 `20` 由「总捐款」改名为**「捐款机累计」**。
  同一批快照也验证了其余各项：累计型（妈妈击杀 / 碎石 / 大便 / 死亡 / 店主击杀）14 个月单调不减，
  状态型（伊甸币、当前连胜）会升会降，均与游戏机制一致。
- **新增 `tools/diff_saves.py`**：只读对比两份存档，输出各进度段的新增/减少（带中文名）与全部变化的计数器
  （下标、旧值 → 新值）。既用来确认上面那两项计数器的口径，也用来实测反推 BOSS 编号
  （打 BOSS 前后各存一份，看 BOSS 段哪一位 0→1）。
- 版本号与 `config_version` 同步升至 `0.2.2`。

## [0.2.1] - 修正小 BOSS / BOSS 段的下标基准，接入七宗罪中文名

- **修正**：小 BOSS 段与 BOSS 段的下标是**从 0 开始算真实条目**的，早期把第 0 位当占位元素是错的。
  依据：REPENTOGON 存档格式记录（小 BOSS 段固定 7 项 = 七宗罪；BOSS 段 104 项 = 按 BOSS id）与公开格式说明，
  并且在真实存档上实测——同一台机器上「空存档位」与「有进度存档位」相比，这两段的第 0 位会随进度 0 → 1，
  而成就 / 道具 / 挑战段的第 0 位恒为 0（占位值不会随进度变化）。
  影响：小 BOSS 图鉴由 `6/6` 变成 **`7/7`**，BOSS 图鉴由 `96/103` 变成 **`97/104`**（同一份存档，实际进度不变）。
- **接入七宗罪中文名**：`assets/isaac_save_names.json` 的 `minibosses` 填入 0~6（懒惰 / 色欲 / 愤怒 / 暴食 /
  贪婪 / 嫉妒 / 傲慢），名称取自游戏本体语言包的 Minibosses 分类；未遇到的罪会以名字列出（解析图与详情都有），
  `tools/build_name_tables.py` 重新生成时也会自动带上这 7 条。
- **BOSS 段仍不给名字**：104 个槽位按游戏内部 BOSS id 排列，暂无可靠公开对照表；现在会明确写出
  「未解锁的 BOSS（N 个）· 编号：…（这一项还没有中文名表，只列编号。）」，不再因为没有名表就整段消失。
- 版本号与 `config_version` 同步升至 `0.2.1`；自检扩到 91 条断言（新增下标基准、修正后总数、合成存档用例）。

## [0.2.0] - 新增存档解析（群文件绑定 + 按 QQ 缓存 + 解析图）

- **新增 `/以撒存档` 系列指令**：用户把存档 `.dat` 传到群文件（或直接发到聊天）后，引用该文件发送
  `/以撒存档绑定`，插件下载校验并**按 QQ 号缓存在 bot 本地**；之后 `/以撒存档` 返回
  **一张进度解析图** + **一条合并转发的详细统计**（`/以撒存档2` 指定存档位、`/以撒存档列表` 看缓存、
  `/以撒存档清除` 删缓存、`/以撒存档详情` 只要文字）。
- **空发即指引**：没有绑定过存档时，`/以撒存档` 会给出「去哪找存档」的完整指引（三个版本的文档目录、
  Steam 云存档与 Proton 路径、`persistentgamedata` 与 `gamestate` 的区别、上传与绑定步骤），走合并转发不刷屏。
- **新增 `isaac_save.py`（纯逻辑，无 SDK 依赖）**：只读解析存档——魔数 + 成就段条目数识别
  胎衣+ / 忏悔 / 忏悔+；按 `count × 实际条目宽度` 跳过数据块（`len` 字段不可信），
  统计从下标 1 开始（第 0 位是占位）；输出成就 / 道具 / BOSS / 小 BOSS / 挑战 / 特殊种子完成度、
  12 项已确认的全局计数器，以及**复用内置图鉴中文名的「还没拿到的道具」清单**。
- **新增 `isaac_save_render.py`**：解析图两条路径——优先把自包含 HTML 卡片交给**宿主自带的浏览器渲染**
  （`ctx.render.html2png`，与宿主渲染同一套，本机实测 1800×1758）；宿主渲染不可用时用 **Pillow 本地绘制**
  等价卡片；两者都不可用则退化为文字摘要，用户不会看到报错。
- **安全与健壮**：`gamestate`（中途退出的单局）、非存档文件、超限文件都会给出明确中文原因；
  下载链接禁止 `file:` 等非 http(s) 协议，并默认拒绝内网 / 回环 / 保留地址（SSRF 防护，`allow_private_url` 可放开）；
  存档按 QQ 号分目录隔离，写入用临时文件 + 原子替换，非持久存档一律拒绝入库。
- **新增 `[save]` 配置节**（14 项，含开关、图片倍率、道具与成就/挑战清单条数、大小与超时上限、缓存有效期、
  名称表与成就详情表路径），`config_version` 升至 `0.2.0`；
  `_manifest.json` 增加 `send.image` / `render.html2png` / `api.call` 能力声明。
- **新增 `assets/isaac_save_names.json`**（成就 / BOSS / 小 BOSS / 挑战「编号 → 中文名」表）与
  `assets/isaac_achievements_zh.json`（成就解锁条件）：目前成就 641 条、挑战 44 条已填充，
  解析结果里会列出**还没完成的挑战**与**还没解锁的成就（含解锁条件）**，解析图里也各带一段名称标签；
  `bosses` / `minibosses` 留空（存档用的是游戏内部 BossID 顺序，无可靠公开来源，不猜），
  留空的段只输出编号与计数。可用配置 `save.name_table_file` / `save.achievement_table_file` 换成自己的表，
  改完重载插件即生效。
- 指令正则调整：`/以撒存档` 系列与 `/以撒 <道具>` 互不抢占（道具指令加负向前瞻）。
- 自检 67 条断言（真实存档解析、指令正则冲突、绑定 / 查询 / 列表 / 清除、坏文件与 `gamestate` 拒绝、
  渲染降级、按 QQ 隔离、SSRF / 协议 / 路径穿越拒绝、真实 HTTP 下载路径），离线 stub SDK 运行；
  另在本机 Steam 真实存档上回归验证：
  忏悔+ 16084 字节 11 段全部对齐，成就 605/641、道具 703/732、BOSS 96/103、挑战 16/45，
  空存档位与忏悔格式同样正确解析。

## [0.1.4] - 补全最后两条 + 新增协同检索

- **515 神秘礼物** 按 wiki 截图补录完成（权重 ｜ 概率 ｜ 事件，含版本差异叠层说明），至此 **13 条表格类条目全部补录**，
  `assets/isaac_extra.json` 的 `meta.pending_review` 已清空。
- **664 大胃王** 修正：原先按网页抓取结果转写的 3 列（食物/伤害/弹速）**列对齐有误**，已按 wiki 表格截图改为完整的
  6 列（食物 ｜ 射速 ｜ 伤害 ｜ 射程 ｜ 弹速 ｜ 幸运），并补一句「修正」含义说明。
- 新增**协同检索** `search.search_by_synergy`（默认开启）：关键词在名称/英文名/编号/标签/效果正文里都匹配不到时，
  会继续在协同条目里找，因此「和 X 有协同的道具」也能被搜出来（如搜「不再叠加」→ 变异蜘蛛；搜「错误王冠」→
  六面骰、大胃王）。优先级排在效果正文检索之后、模糊匹配之前。
- 自检扩到 55 条断言（新增协同检索开关、降级提示兜底用例）。

## [0.1.3] - 补全 12 条表格类条目

- 逐条从灰机 wiki 转写补录，共 **12 条**：75 药学博士证、263 透明符文、286 空白卡牌、297 潘多拉魔盒、
  303 室女座、348 安慰剂、373 死亡之眼、619 长子名分、654 伪造药学博士证、658 巨细胞、664 大胃王、683 骨刺。
  形式包含 10 张表格（共 100 行）与 43 条补充说明（其中 619 长子名分为 35 条角色专属效果）。
- **515 神秘礼物** 的源表结构复杂、抓取结果不稳定，暂未补录，仍走「附 wiki 链接」的降级提示；
  664 大胃王的列对齐存在歧义，建议人工核对（两条都已记入 `assets/isaac_extra.json` 的 `meta.pending_review`）。
- 工具路径新增补录内容预览：`isaac_item_lookup(detail=false)` 时补录表格/补充说明只列前 3 行并注明总条数，
  `detail=true` 返回全部，避免长列表（如长子名分 35 条）直接灌进模型上下文。
- 有补录内容的条目不再重复附 wiki 链接提示。

## [0.1.2] - 补录 wiki 表格类效果

- **修复表格丢失**：wiki 用表格描述效果的道具（潘多拉魔盒等）此前只显示到「……触发以下效果：」就断了。
  根因是上游爬虫 `crawl_wiki_v2.py` 只提取页面里的 `<li>` 列表项、**从不解析 `<table>`**，
  而 `description` 字段又被统一截断在 200 字符，无法回捞。
- 新增**人工补录机制** `assets/isaac_extra.json`（`tools/build_data.py --extra` 自动合并，默认路径即该文件）：
  支持按图鉴编号补 `table`（title/columns/rows/source）与 `extra_lines`；补录内容随数据集一起打包，运行时仍不联网。
- 已补录 **潘多拉魔盒（297）** 的「根据当前楼层触发以下效果」表（16 行，含贪婪模式列）与 2 条补充说明。
- 未补录的表格类条目会附一行提示与灰机 wiki 页面链接，新增配置 `display.show_wiki_link`（默认开启）控制。
- `tools/build_data.py` 的 docstring 与输出补充了「表格缺失」的成因说明与补录步骤。

## [0.1.1] - 协同信息不再折叠

- **协同超过内联上限时改用合并转发**：详情里内联展示的协同条数超过 `display.max_synergies`（默认 5）时，
  不再只列前几条并提示「用 /以撒协同 查看全部」，而是正文留一行提示、**全部协同用一条合并转发发送**；
  合并转发不可用时自动回退为普通文本（内容不丢）。
- 新增配置 `display.max_synergies_in_tool`（默认 20）：LLM 工具无法发合并转发，超出该条数时按截断处理并注明
  「共 N 条，此处列出前 M 条」。
- `display.max_synergies` 语义调整为「内联上限」：`0` 表示全部内联展示（不转发）。

## [0.1.0] - 首个版本

- 新增 `/以撒` 系列指令：查询、详情、协同、编号、随机、帮助。
- 新增 LLM 工具 `isaac_item_lookup`，支持模型在对话中自行查询图鉴。
- 内置离线数据集 `assets/isaac_items.json`：1039 条（道具 715 / 饰品 183 / 卡牌 94 / 药丸 47），
  含品质、标签、效果正文、套装与 3599 条协同；由 `tools/build_data.py` 从上游开源项目生成。
- 检索支持编号、中文名、英文名、标签、效果关键词与模糊匹配（阈值可配置）。
- 长内容超过 `display.forward_threshold` 时自动改用合并转发，失败回退普通文本；四节配置（插件 / 检索 / 展示 / 数据）均可在 WebUI 调整。
