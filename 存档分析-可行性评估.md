# 存档分析功能 — 可行性评估

> 评估对象：`cateye_isaac_item`（以撒道具图鉴插件）
> 评估日期：2026-09-16
> 评估方式：**只读**调研 + 在本机真实存档上实跑解析验证，未改动插件任何代码
> 结论：**可行，且实现难度低于预期**

---

## 〇、实现状态（2026-09-16 更新，插件 v0.2.1）

本评估的「阶段一：只读核心」已按**另一条投递路线**实现并通过真实存档回归验证：

| 评估文档的建议 | 实际实现 |
|---|---|
| 插件在宿主机本地找存档（Documents + Steam 云双路径） | **改为用户主动提供**：用户把 `.dat` 传到群文件（或直接发到聊天）后引用并发送 `/以撒存档绑定`，插件下载校验后按 **QQ 号**缓存在 `ctx.paths.data_dir/saves/<QQ>/slotN.dat` |
| `isaac_save.py`：探测 → 解析 11 段 → 版本识别 | 已实现（`isaac_save.py`，纯逻辑、无 SDK 依赖、可离线单测），并增加「按文件名查群文件取链接」「SSRF / 大小 / 超时校验」「按 QQ 分目录缓存与清理」 |
| 输出成就 / 道具发现 / BOSS / 挑战 / 全局统计 | 已实现：解析图（进度条 + 统计卡 + 未发现道具标签）+ 合并转发详情（含未发现道具中文名，复用内置图鉴 715 条道具名） |
| 指令 `/以撒存档 [存档位]` + LLM 工具 | 指令已实现：`/以撒存档`、`/以撒存档绑定`、`/以撒存档N`、`/以撒存档列表`、`/以撒存档清除`、`/以撒存档详情`；空发时给出「去哪找存档」指引；LLM 工具**未加**（工具发不了图与转发，收益低，留待后续） |
| 配置新增 `[save]` 节 + bump `config_version` | 已实现（14 项配置，`config_version` 0.1.4 → 0.2.1） |
| 只读、不改存档 | 严格只读：只解析字节流；写入仅限插件自己的缓存目录 |
| 渲染 | 评估文档未涉及；实现为 **Pillow 本地绘制**（内置 Noto Sans SC 子集字体、宽度与倍率可调），Pillow 缺失时降级为纯文字。宿主 `render.html2png` 实测常超时，故不再使用 |

**真实存档回归结果**（本机 Steam 云存档）：`rep+persistentgamedata1.dat` 16084 字节
11 段全部对齐，成就 605/641、道具 703/732、BOSS 97/104、小 BOSS（七宗罪）7/7、挑战 16/45、
特殊种子 0/79，计数器 12 项已确认值全部读出；另外两种文件（空存档位、忏悔格式存档）与
`gamestate` 拒绝分支也已验证。

> **勘误（v0.2.1）**：本文第 3.3 节「数组第 0 位是占位元素」只对**成就 / 道具 / 挑战 / 特殊种子**成立
> （官方格式说明：``index 0 is unused``）；**小 BOSS 段（固定 7 项 = 七宗罪）与 BOSS 段
> （104 项，按 BOSS id）的第 0 位是真实条目**，总数就是 ``count``。依据：REPENTOGON 的
> `SaveFile.h` 逐段记录与公开格式说明，加上实测——同一台机器上「空存档位」与「有进度存档位」
> 比较，这两段的第 0 位随进度 0 → 1，而占位段的第 0 位恒为 0。
> 修正后同一份存档的显示由「BOSS 96/103、小 BOSS 6/6」变为 **「BOSS 97/104、小 BOSS 7/7」**
> （实际进度不变，只是分母与分子不再各少 1）。小 BOSS 的七宗罪中文名
> （懒惰 / 色欲 / 愤怒 / 暴食 / 贪婪 / 嫉妒 / 傲慢）已接入名称表。

**尚待补全（BOSS 名表已于 2026-09-16 补齐，见附录 B）**：BOSS 段 104 个槽位的下标就是
游戏 `entities2.xml` 里的 `bossID` 属性，已解出归档、生成名表（`assets/isaac_boss_slots.json`，
100 个具名 + 4 条说明）。成就 / 挑战 / 小 BOSS / BOSS 的名表**均已接入**：
`assets/isaac_save_names.json`（成就 641 / 挑战 44 / 小 BOSS 7 / BOSS 104）+
`assets/isaac_achievements.json`（成就名与解锁条件，v0.2.4 起改由游戏本体
`achievements.xml` + 官方语言包生成），因此详情里能直接给出
「还差哪些成就、怎么解锁」「还没完成哪些挑战」「还没遇到哪些小 BOSS」「存档没有记录哪些 BOSS」。

---

## 一、结论摘要

| 维度 | 评估 |
|---|---|
| 格式是否公开 | 是。已有完整的 Kaitai Struct 格式定义，且格式本身**无加密、无压缩** |
| 是否需第三方依赖 | 不需要。纯标准库手写解析器约 150 行，可完全不引入依赖 |
| 是否有现成 Python 库 | **没有**。PyPI 上无任何相关包，现有实现清一色 JS/TS |
| 数据能否复用 | 部分能。插件现有 715 条道具数据正好是编号索引的，可直接用于「道具发现」 |
| 主要工作量 | 不在解析，而在**补数据表**（成就名、BOSS 名、挑战名） |
| 主要风险 | **许可冲突**（参考项目为 GPL-3.0 / 无许可），以及「MaiBot 与游戏是否同机」 |
| 建议范围 | v1 做**只读**，先上成就 / 道具发现 / BOSS 记录 / 挑战 / 全局统计五项 |

**可行性已验证**：在本机 Steam 云存档上实跑，11 个数据块全部按顺序正确解析，读出的统计数字（妈妈击杀 226、死亡 438、碎石 59304、店主击杀 58）均与游戏内表现相符。

---

## 二、参考项目链接

### 2.1 存档解析工具（实现参考）

| 链接 | 说明 | 许可 |
|---|---|---|
| https://github.com/Zamiell/isaac-save-viewer | 最完整的只读解析器，TypeScript。**主要参考对象** | GPL-3.0 |
| https://zamiell.github.io/isaac-save-viewer/ | 上述项目的在线版，可直接丢存档看效果 | — |
| https://github.com/Demorck/Isaac-save-manager | 读写双向，覆盖统计 / 图鉴 / 角色标记，字段最全 | **未声明 LICENSE** |
| https://github.com/Zamiell/isaac-save-installer | 同作者的存档安装器，仓库内含官方存档样本 `.dat`，可作测试数据 | GPL-3.0 |

### 2.2 格式规格（最关键，优先看）

| 链接 | 说明 |
|---|---|
| https://github.com/Zamiell/isaac-save-viewer/blob/main/static/lib/IsaacSaveFile.ksy | **权威二进制规格**。本评估的字节布局全部照它验证。4418 字节 |
| https://github.com/Zamiell/isaac-save-viewer/blob/main/src/readFile.ts | 解析入口。含三种文件头与「胎衣+ 与忏悔共用 `09R` 头」的区分逻辑 |
| https://github.com/Zamiell/isaac-save-viewer/blob/main/src/fillPage.ts | 各字段的实际用法与渲染逻辑 |
| https://github.com/Zamiell/isaac-save-viewer/blob/main/src/enums/ChunkType.ts | 数据块类型枚举（1~11） |
| https://github.com/Demorck/Isaac-save-manager/blob/main/js/Helpers/Statistics_offset.ts | **全局计数器下标映射**，把死亡次数、妈妈击杀、碎石数等定位到具体下标 |
| https://github.com/Demorck/Isaac-save-manager/blob/main/js/Helpers/Constants.ts | 各版本成就上限、CRC32 表、`CHECKSUM = 0xFEDCBA76`（**仅写入时需要**） |
| https://github.com/Demorck/Isaac-save-manager/blob/main/js/Models/Save.ts | 存档数据模型与图鉴（bestiary）处理方式 |

### 2.3 中文资料

| 链接 | 说明 |
|---|---|
| https://isaac.huijiwiki.com/wiki/Project:%E5%AD%98%E6%A1%A3%E8%AE%BE%E7%BD%AE | 存档设置页，含各版本存档路径与支持范围 |
| https://isaac.huijiwiki.com/wiki/Project:%E5%AD%98%E6%A1%A3/%E8%AE%A1%E6%95%B0%E5%99%A8%E6%9F%A5%E8%AF%A2 | **计数器查询页**，唯一的中文下标对照资料，建议作为数据表来源 |
| https://aprisyourlie.github.io/IsaacAchievementGuide/ | 成就中文名与解锁条件整理；其**数据**源自英文 wiki（CC BY-SA 4.0）与灰机 wiki（CC BY-NC-SA 3.0），仓库的 GPL-3.0 只覆盖其代码。v0.2.5 起本插件按 CC 许可引入该数据文件（署名 + 非商业），代码仍 MIT |

> ⚠️ 灰机 wiki 有 Cloudflare 保护：`curl`（即便带完整浏览器头）与 `action=raw` / `action=parse` 均返回 403 挑战页。需用浏览器打开，或走 `WebFetch` 类渲染通道。**适合一次性人工补录，不适合做成自动构建步骤。**

### 2.4 格式逆向来源与工具链

| 链接 | 说明 |
|---|---|
| https://github.com/bladecoding | 格式逆向作者（Blade），使用 Kaitai Struct 完成 |
| https://kaitai.io/ | Kaitai Struct 官网 |
| https://ide.kaitai.io/ | 在线 IDE，丢 `.ksy` 进去可直接生成 Python 解析器 |
| https://github.com/kaitai-io/kaitai_struct | 编译器仓库 |
| https://github.com/kaitai-io/kaitai_struct_formats | 格式库（已确认**不含**以撒，规格只能从 2.2 拿） |

**资源归档（`.a`）的格式来源**（2026-09-16 为解出 `entities2.xml` 时新增）：

| 链接 | 说明 | 许可 |
|---|---|---|
| https://www.zenhax.com/viewtopic.php?t=1676 | Ekey 给出的 `AHeader` / `AEntry` 结构体与「版本 2 = DEFLATE（分块）」结论 | 论坛讨论（无许可证，属事实性信息） |
| https://github.com/gibbed/Gibbed.Rebirth | Gibbed 的 `ArchiveFile.cs`，同格式的另一份实现记录 | **Zlib** |
| http://aluigi.altervista.org/bms/arch000.bms | QuickBMS 脚本（作者本人标注「不完整」），可确认「目录表在尾部、数据按 `[u32 zsize]` 分块」 | QuickBMS 脚本库（仅作对照） |

### 2.5 其他相关

| 链接 | 说明 |
|---|---|
| https://cuerzor.github.io/IsaacDocs/rep/tutorials/directories-and-save-files.html | 存档目录结构说明（Lua API 文档） |
| https://isaacscript.github.io/ | IsaacScript 模组框架，查 ID 常量表有用 |
| https://github.com/Rchardon/RebirthItemTracker | Zamiell 项目的道具 JSON 来源 |
| https://github.com/ChenDekang617/isaac-item-recommender | **插件现有数据的上游** |
| https://github.com/cateyemizuki/cateye_isaac_item | 本插件仓库 |

---

## 三、存档文件格式

### 3.1 文件位置

存档可能出现在**两处**，功能必须都找（实测：本机实时存档在 Steam 云目录，而非 Documents）：

| 位置 | 路径 |
|---|---|
| 本地文档 | `%USERPROFILE%\Documents\My Games\Binding of Isaac Repentance+`（忏悔+）<br>`%USERPROFILE%\Documents\My Games\Binding of Isaac Repentance`（忏悔） |
| Steam 云存档 | `<Steam 安装目录>\userdata\<SteamID>\250900\remote\` |

> 实测本机：`Documents\My Games\Binding of Isaac Repentance+` 下**只有 `save_backups` 历史快照，没有实时存档**；游戏实际读写的是 `D:\Steam\userdata\843306148\250900\remote\`。只查 Documents 会读不到数据。

**文件名**（`N` 为存档位 1~3）：

| 版本 | 持久存档 | 进行中的一局 |
|---|---|---|
| 忏悔+ | `rep+persistentgamedataN.dat` | `rep+gamestateN.dat` |
| 忏悔 | `rep_persistentgamedataN.dat` | `rep_gamestateN.dat` |
| 胎衣+ | `abp_persistentgamedataN.dat` | `ab_gamestateN.dat` |

> ⚠️ `gamestate` 是「中途退出的单局状态」，**不是**持久存档，插件必须识别并明确拒绝（魔数形如 `ISAACNG_GSR0142`），否则会给用户错误结论。

### 3.2 整体布局

```
偏移 0    : 16 字节魔数  'ISAACNGSAVE09R  '   （注意尾部两个空格）
偏移 16   : s4  CRC32（小端）。只读分析可忽略；写入才需重算
偏移 20   : 11 个数据块依次排列
```

每个数据块的头部：

```
type   s4   块类型 1..11
len    s4   长度字段 —— ⚠️ 不可靠，见下
count  s4   条目数
body        数据体
```

**⚠️ 关于 `len` 字段（实测结论）**：它不是数据体的真实字节数。实测规律是 —— 成就段 `len = count × 1`，其余各段 `len = count × 4`，**与实际条目宽度无关**（道具段每条 1 字节但 `len = count × 4`）。因此：

- **跳过数据块必须用 `count × 实际条目宽度` 计算**
- `len` 只能用来做一致性校验，不能用来定位下一段
- 上游 `.ksy` 也标注了 `This tends to be wrong`

### 3.3 各数据块明细

| type | 名称 | 条目宽度 | 数据体布局 | v1 是否采用 |
|---|---|---|---|---|
| 1 | achievements | 1 字节 | `count` 个 u1（0/1） | ✅ |
| 2 | counters | 4 字节 | `count` 个 s4 | ✅（前 24 项） |
| 3 | level_counters | 4 字节 | `count` 个 s4 | ⭕ 可选 |
| 4 | collectibles | 1 字节 | `count` 个 u1 | ✅ |
| 5 | minibosses | 1 字节 | `count` 个 u1 | ✅ |
| 6 | bosses | 1 字节 | `count` 个 u1 | ✅ |
| 7 | challenge_counters | 1 字节 | `count` 个 u1 | ✅ |
| 8 | cutscene_counters | 4 字节 | `count` 个 s4 | ❌ 跳过 |
| 9 | game_settings | 4 字节 | `count` 个 s4 | ❌ 跳过 |
| 10 | special_seed_counters | 1 字节 | `count` 个 u1 | ⭕ 可选（彩蛋种子） |
| 11 | bestiary_counters | 变长 | `count` 组：`(type s4, count s4, count/4 个 (entity s4, value s4))` | ❌ **v1 不做** |

**⚠️ 数组的下标基准按段区分（v0.2.1 修正，详见第〇节勘误）。** 成就 / 道具 / 挑战 / 特殊种子的第 0 位未使用（`index 0 is unused`），真实总数 = `count − 1`，统计从下标 1 开始；**小 BOSS 段（固定 7 项 = 七宗罪）与 BOSS 段（104 项，按 BOSS id）的第 0 位是真实条目**，总数 = `count`，统计从下标 0 开始。

**⚠️ bestiary（type 11）结构未完全摸清**：实测按上述规则解析可正确走到文件尾部附近，但仍有 **8 字节未被消费**，且 `len` 字段与实际数据体差距很大。该段还有未文档化的细节，且收益最低，**建议 v1 明确不支持**。

### 3.4 全局计数器下标映射（type 2）

下标 = 字节偏移 ÷ 4。已确认含义的 14 项：

| 下标 | 含义 | 本机实测值 |
|---|---|---|
| 0 | 捐款机 | 0 |
| 1 | 妈妈击杀 | 226 |
| 2 | 碎石数 | 59304 |
| 3 | 染色石破坏 | 787 |
| 4 | 未命名 | 7 |
| 5 | 大便破坏 | 24948 |
| 6~9 | 未命名 | — |
| 10 | 死亡次数 | 438 |
| 11 | 店主击杀 | 58 |
| 12~18 | 未命名 | — |
| 19 | 捐款币 | 0 |
| 20 | 总捐款 | 1092 |
| 21 | 伊甸币 | 223 |
| 22 | 当前连胜 | 0 |
| 23 | 最佳连胜 | 11 |

> 该段共 523 项，除上述统计外还包含**各角色完成标记、罪恶等数据**，但**没有公开的完整对照表**。这是本功能最大的数据缺口，v1 建议只输出已确认的 14 项统计。

### 3.5 版本识别

魔数区分大版本：

| 魔数 | 版本 |
|---|---|
| `ISAACNGSAVE06R` | Rebirth |
| `ISAACNGSAVE08R` | Afterbirth |
| `ISAACNGSAVE09R` | Afterbirth+ / Repentance / Repentance+ |

**胎衣+ 与忏悔共用 `09R` 头**，必须用成就段的 `count` 再区分：

| 成就段 count | 版本 |
|---|---|
| 404 | 胎衣+（403 个成就 + 占位） |
| 638 | 忏悔（637 + 占位） |
| 642 | 忏悔+ / 在线版（641 + 占位） |

### 3.6 路径处理的坑

存档路径含**空格**与 **`+`** 号，且中文 Windows 用户名常见。读取时一律用 `pathlib.Path`，避免字符串拼接。

---

## 四、本机实测数据

测试文件：`D:\Steam\userdata\843306148\250900\remote\rep+persistentgamedata1.dat`（16084 字节，忏悔+）

| 项目 | 实测 |
|---|---|
| 成就 | 605 / 641 |
| 道具发现 | 703 / 732 |
| BOSS 记录 | 96 / 103 |
| 小 BOSS 记录 | 6 / 6 |
| 挑战 | 16 / 45 |
| 特殊种子 | 0 / 79 |
| 解析结果 | 11 段全部对齐，落点 16076（距文件尾 8 字节，未确认用途） |

另两个存档位（`rep+persistentgamedata2.dat`、`rep_persistentgamedata1.dat`）均为**空存档**，同样能正确解析——说明空存档与满存档走同一套逻辑，无需特判。

---

## 五、可复用资产与数据缺口

### 5.1 可直接复用

插件现有 `assets/isaac_items.json` 的 715 条道具**正好按编号索引**，与存档的 `collectibles` 段天然对应：

- 实测：29 个未发现道具中，**20 个能立刻给出中文名**（彼列之书、死亡证明、稻草人、肾结石、科技 0.5、透明符文……）
- 剩余 9 个（编号 235 / 587 / 613 / 620 / 630 / 648 / 662 / 666 / 718）是空槽位或图鉴缺项，跳过即可
- 反向也成立：插件中编号 ≥ 733 的条目（1119 / 2068 / 3031 / 3038）是饰品卡牌药丸，不应参与道具统计

**这是本功能最划算的部分——不需要任何新数据就能做出「未发现道具清单」。**

### 5.2 需要新建的数据表

| 数据表 | 规模 | 难度 | 来源建议 |
|---|---|---|---|
| 成就 ID → 中文名 | 641 条 | 中 | 灰机 wiki 成就页 |
| BOSS ID → 中文名 | 103 条 | 中 | 灰机 wiki 怪物页 |
| 小 BOSS ID → 中文名 | 6 条 | 低 | 同上 |
| 挑战 ID → 中文名 | 45 条 | 低 | 灰机 wiki 挑战页 |
| 计数器下标含义 | 已确认 14 项 | 已完成 | 见 3.4 |
| 图鉴实体 ID → 名称 | 数百条 | **高** | v1 不做 |

> 建议沿用插件既有的 `assets/isaac_extra.json` 补录模式：人工补录放独立文件，构建脚本合并进主数据，并在条目上标注 `meta.pending_review`，**不要把不确定的数据当确定数据发出去**。

---

## 六、改动面

| 文件 | 改动 |
|---|---|
| `isaac_save.py`（**新增**） | 纯逻辑模块：定位 / 解析 / 映射 / 排版。不依赖 `maibot_sdk`，与现有 `isaac_data.py` 完全对称，可离线单测 |
| `plugin.py` | 新增指令 `/以撒存档`（别名 `/以撒进度`）与 LLM 工具 `isaac_save_progress` |
| 配置 | 新增 `[save]` 节：`enabled`、`save_dir`（留空自动探测）、`slot`、`cache_ttl` |
| `plugin.config_version` | **必须 bump**。宿主的配置迁移靠比较文件内 `config_version` 与模型默认值，不改则老用户永远拿不到新字段 |
| `_manifest.json` | 版本号 + 描述更新。**capabilities 无需新增** —— 文件读取是普通 Python IO，不走能力白名单 |
| `assets/` | 新增成就 / BOSS / 挑战数据表 |
| `README.md` / `COMMANDS.md` / `CHANGELOG.md` | 补文档 |

**关于目录约定**：宿主插件评审要求「插件自有数据放 `ctx.paths.data_dir`」。但**读用户游戏存档**属于读取用户指定的外部路径，与「插件自有数据」不同，应通过配置项显式指定，并在帮助文本里说明插件只读不写。

---

## 七、风险与坑

按严重度排序：

1. **许可冲突（最需先决策）**
   参考项目为 **GPL-3.0**（Zamiell）与**未声明许可**（Demorck），本插件是 MIT。**不能直接复制它们的 JSON / TS 数据文件**，否则污染插件授权。
   - ✅ 格式规格（字节布局、魔数、下标含义）属于事实信息，自己实现无问题
   - ❌ 成就名、BOSS 名等数据表应从灰机 wiki 或游戏本体自行整理，或向作者取得授权

2. **部署前提：必须同机**
   插件跑在 MaiBot 的 Runner 子进程里，只能读**宿主机本地**文件。若 MaiBot 部署在 Docker / WSL / 另一台机器，就读不到存档 —— 必须支持手动填路径，并在读不到时给出清晰提示（而不是静默失败）。

3. **只读 vs 可写**
   写入需要重算 CRC32（`CHECKSUM = 0xFEDCBA76`）。**强烈建议 v1 严格只读**，风险降低一个量级，也避免误改用户存档引发纠纷。

4. **版本兼容**
   见 3.5。需处理三种魔数、`09R` 的双版本歧义、`gamestate` 文件的误识别。

5. **`len` 字段不可靠**
   见 3.2。用 `count × 条目宽度` 定位下一段。

6. **数组第 0 位占位**
   见 3.3。占位段从下标 1 开始统计，小 BOSS / BOSS 段从下标 0 开始。

7. **bestiary 段结构未完全摸清**
   见 3.3。v1 明确不支持，遇到时告知用户而非输出错误数据。

8. **缓存与性能**
   存档可达数百 KB，建议用 `asyncio.to_thread` 读取（与现有代码习惯一致），并按文件 `mtime` 做缓存，避免每条消息都重解析。

---

## 八、建议实施方案

### 阶段一：只读核心（建议先做）

- [ ] `isaac_save.py`：路径自动探测（Documents + Steam 云，双路径）→ 解析 11 段 → 版本识别
- [ ] 输出：成就 / 道具发现（复用现有中文名）/ BOSS 记录 / 挑战 / 全局统计 5 项
- [ ] 指令 `/以撒存档 [存档位]`，LLM 工具 `isaac_save_progress`
- [ ] 配置 `[save]` 节 + bump `config_version`
- [ ] 在真实存档上验证三种版本（空存档 / 满存档 / 不同版本）

### 阶段二：数据补全

- [ ] 从灰机 wiki 补录成就名（641）、BOSS 名（103）、小 BOSS 名（6）、挑战名（45）
- [ ] 未发现项清单（「还差这 36 个成就没解锁」）

### 阶段三：增值玩法（可选）

- [ ] **进度对比**：`save_backups` 里有数月的每日快照，可做「最近一周解锁了什么」
- [ ] 计数器扩展：角色完成标记（需先解决 3.4 的数据缺口）
- [ ] 彩蛋种子收集进度

### 明确不做

- ❌ 修改存档（写入）
- ❌ bestiary 怪物图鉴（结构未摸清）
- ❌ 需要联网的任何能力（违背插件「离线可用」的既有定位）

---

## 九、许可注意事项

| 来源 | 许可 | 可用方式 |
|---|---|---|
| Zamiell/isaac-save-viewer | GPL-3.0 | 仅参考**格式规格**与实现思路；不可复制代码或数据文件 |
| TeamREPENTOGON/REPENTOGON | GPL-2.0 | 仅参考其格式记录（`SaveSyncing/SaveFile.h`）里的**字节布局事实**；不可复制代码 |
| Demorck/Isaac-save-manager | 未声明 | 视为「保留所有权利」，仅参考思路 |
| gibbed/Gibbed.Rebirth | **Zlib**（宽松） | 归档结构体字段属事实信息；本项目按其字段重新实现，**未复制代码**，README 已署名并注明许可 |
| zenhax 论坛帖（Ekey）、QuickBMS `arch000.bms` | 无许可证 / 脚本库 | 只用其中「格式长什么样」的事实（字段顺序、块划分），不使用其代码 |
| 灰机 wiki | 通常 CC BY-NC-SA 系 | 数据可用于非商业用途，**需标注来源**；商业使用需自行确认授权 |
| Kaitai `.ksy` 格式定义 | 随上述仓库 | 二进制布局属事实信息，可自行重新实现 |
| aprisyourlie/IsaacAchievementGuide | 仓库标注 **GPL-3.0**（仅代码） | 其 `THIRD_PARTY_NOTICES.md` 声明数据来自 wiki：英文 wiki **CC BY-SA 4.0**、灰机 wiki **CC BY-NC-SA 3.0**。插件按 **CC 许可**引入该数据文件（`assets/isaac_achievements_zh.json`，署名 + 非商业），代码保持 MIT（聚合分发）。**不建议把插件整体改 GPL-3.0**：NC 与 GPL 不兼容，改了也救不了这份数据 |

插件现有 `README.md` 已对上游数据做了许可说明，**新增的存档分析数据表也必须照此办理**，并在 README 里如实标注来源与可信度。
`tools/check_licenses.py` 会扫描产物里的传染性许可标记、核对 LICENSE 与 manifest、并逐文件登记来源（自检里也有防回归断言），
因此「整包可按 MIT 开源」这件事是可复核的，而不是一句声明。

本次新增产的许可归属（2026-09-16 v0.2.3）：`assets/isaac_boss_slots.json`（BOSS 槽位对照表）与
`tools/archive_reader.py`（归档解码器）**不含任何第三方代码或第三方整理的数据**——
BOSS 中文名取自玩家自装游戏的官方语言包，槽位下标取自游戏自己的 `entities2.xml`，
归档格式为自行实现（仅参考上表中的事实性字段定义）。因此仍与本插件一致按 **MIT** 发布。

---

## 附录 A：解析参考实现（核心骨架）

```python
import struct
from pathlib import Path

MAGIC = b"ISAACNGSAVE09R  "
SAVE_HEADER_LEN = 20          # 16 字节魔数 + 4 字节 CRC32
CHUNK_WIDTH = {               # type -> 单条目字节数（bestiary 变长，单独处理）
    1: 1, 2: 4, 3: 4, 4: 1, 5: 1, 6: 1,
    7: 1, 8: 4, 9: 4, 10: 1,
}

def parse_save(path: Path) -> dict:
    raw = path.read_bytes()
    if raw[:16] != MAGIC:
        raise ValueError(f"不是忏悔存档（魔数 {raw[:16]!r}）")

    off = SAVE_HEADER_LEN
    chunks: dict[int, tuple[int, int]] = {}   # type -> (count, body_offset)

    for _ in range(11):
        ctype, _length, count = struct.unpack_from("<iii", raw, off)
        off += 12                              # type + len + count
        chunks[ctype] = (count, off)

        if ctype == 11:                        # bestiary：变长，逐组跳过
            for _ in range(count):
                _bt, bc = struct.unpack_from("<ii", raw, off)
                off += 8 + (bc // 4) * 8
        else:
            off += count * CHUNK_WIDTH[ctype]  # 用 count，不用 len

    return {
        "achievements": _flags(raw, chunks, 1),   # 下标 1 起才是真实条目
        "collectibles": _flags(raw, chunks, 4),
        "bosses":       _flags(raw, chunks, 6),
        "minibosses":   _flags(raw, chunks, 5),
        "challenges":   _flags(raw, chunks, 7),
        "counters":     _counters(raw, chunks, 2),
        "version":      _detect_version(raw, chunks),
    }

def _flags(raw, chunks, ctype):
    count, body = chunks[ctype]
    return [i for i in range(1, count) if raw[body + i]]

def _counters(raw, chunks, ctype):
    count, body = chunks[ctype]
    return struct.unpack_from(f"<{count}i", raw, body)

def _detect_version(raw, chunks) -> str:
    count = chunks[1][0]
    return {404: "afterbirth+", 638: "repentance", 642: "repentance+"}.get(count, "unknown")
```

要点复述：**用 `count × 宽度` 跳过数据块**（`len` 字段不可靠）、**按下标基准统计**（成就 / 道具 / 挑战 / 特殊种子的第 0 位未使用，小 BOSS / BOSS 段第 0 位是真实条目，见 3.3）、**魔数只用于区分大版本**（胎衣+ 与忏悔要靠成就数再分）。

---

## 附录 B：BOSS 名表（2026-09-16 **已解决**，含一次失败路线的记录）

**目标**：给存档 BOSS 段的 104 个槽位配上中文名。

### B.1 先走过的弯路（失败，保留备查）

第一轮假设「槽位 = 把游戏实体按 `(type, variant)` 升序排列后的第 N 个」，用
*Enhanced Boss Bars* 模组的 `bossbar_data.lua`（177 条 `实体类型.变体`）当候选清单、
用图鉴段当「遇到过」集合做贪心对齐，得到一个 104 槽位的候选解。

**回测不通过**：用它预测 93 份历史快照的 BOSS 位图只有 **63 份一致**（最差错 45 位），
且它把**以撒（102.0）、死寂（407.0）、母亲（912.0）、野兽（951.0）**这些玩家必然打过的
BOSS 排除在清单外，反而选进 `102.2` / `408.0` / `266.1` 这类变体。

⇒ 结论：**顺序既不是实体 type，也不是 `(type, variant)` 升序**。这条路彻底作废。

### B.2 真正的答案：`entities2.xml` 的 `bossID`

存档 BOSS 段的 104 个字节，**下标就是 `entities2.xml` 里实体的 `bossID` 属性**
——它是独立于实体类型的一套编号（例：`#MONSTRO` 是 `id=20`、`bossID=1`；
`#ULTRA_GREED` 是 `id=406`、`bossID=62`）。

拿到这份文件的障碍是它打包在 `.a` 归档里，而归档的块格式没有公开说明。最终解出的格式：

```
AHeader: "ARCH000"(7) + u8 version + u32 tableOffset + u16 entryCount
AEntry : u32 hashA; u32 hashB; u32 offset; u32 size; u32 crc;   // 目录表在文件尾部
data   : entry.offset 起，按块存 [u32 zsize][zsize 字节]，直到凑满 size
```

版本 2（胎衣+ / 忏悔 / 忏悔+ 的 `afterbirthp.a`、`repentance.a`）的块判据是关键：
解压端**每块产出 1024 字节**，所以 `zsize == 该块应有输出长度` 时该块是**原样存储**，
否则这 `zsize` 字节是一个**裸 deflate 流**（`zsize` 最高位是「最后一块」标记，不参与长度）。
——这条判据不能换成「试着 inflate，成功就当压缩块」：PCM / WAV 一类数据偶尔能碰巧解出内容，
会算错长度。按此规则，`afterbirthp.a` **10228/10228**、`repentance.a` **4180/4180** 条条目
全部解码成功且长度与目录表声明一致（工具：`tools/archive_reader.py`）。
类型 1（LZW，`config.a` 等）与类型 0（加密，语言包）未实现，工具会明确报错。

> 注：官方 `tools/ResourceExtractor.exe` 在当前版本上会崩溃（`0xC0000374` / `0xC0000005`），
> 且它依赖 `filelist.txt` 反查文件名（归档里只存两个 32 位名字哈希），所以本仓库改为**按内容**查找条目。

### B.3 证据链（`tools/build_boss_table.py` 每次都会重跑前两条）

| # | 证据 |
|---|---|
| 1 | `bossID` 取值 1–110，其中 **89、90、103、104–109 没有任何实体占用**；0–103 范围内的空号（89、90、103）在**所有**存档里恒为 0 |
| 2 | 胎衣+ 存档的 BOSS 段只有 72 / 73 槽（当时 `bossID` 上限更低），其位模式与忏悔+ 存档的**前缀逐位相同**——下标空间稳定，高位槽位是后续版本才加的 |
| 3 | 100 个已分配槽位里 97 项在一份「Dead God」存档里置位；剩下 3 项中，**62（究极贪婪）与 71（究极大贪婪）图鉴击杀 162 / 88 次却仍未置位**——游戏本身不写这两项（贪婪模式专用 BOSS 不走那套标记逻辑） |
| 4 | 72（胖蛆族母）图鉴遭遇 114 次、击杀 0 次（无法被正常击败）；98（绷带巫妖）在任何存档里都没出现过 |
| 5 | 槽位 0 在所有有进度的存档里都是 1，但当前 `entities2.xml` 没有 `bossID=0` 的实体（胎衣时代留下的保留位），因此只做说明、不给名字 |

> 证据 3、4 用到的「100% 存档」是公开的
> [Zamiell/isaac-save-installer](https://github.com/Zamiell/isaac-save-installer) 里的
> `saves/Repentance/persistentgamedata.dat` 与 `saves/Repentance+/persistentgamedata.dat` 两份
> （该仓库为 GPL-3.0；这里只**本地下载用于核对**，没有任何字节进入插件产物）。
> 它们与用户存档的位模式**逐位相同**（97/104，未置位都是 62、71、72、89、90、98、103），
> 而这两份「Dead God」存档里 406.0 / 406.1 的图鉴击杀为 162 / 88 —— 于是坐实了
> 「游戏不写 62 / 71 这两个槽位」。

### B.4 交付物

- `tools/archive_reader.py`：`.a` 归档只读解码器（`list` / `check` / `find` / `extract`）；
- `tools/build_boss_table.py`：由 `entities2.xml` 生成名表，**自带验证**（结构 + 逐份存档核对
  「未分配槽位恒为 0」，并给出「已击杀却不置位」的证据），验证不过就**不写任何名称表**（退出码 2）；
- `assets/isaac_boss_slots.json`：104 槽位逐条明细（bossID / 实体 `id.variant` / 英文名 / 官方中文名 /
  状态 / 说明 / 证据）；
- `assets/isaac_save_names.json` 的 `bosses` 段：100 个具名 + 4 条纯说明（89 / 90 / 103 未分配空槽位、
  0 保留位），其中 62 / 71 / 72 / 98 带「游戏不写这一项 / 无法被击败 / 从未出现」的说明，
  避免把游戏自身的行为误报成「你没打过」。

### B.5 顺带确认的两件事

1. **图鉴段（第 11 段）结构已完全解出**：`[sectionID][sectionSize][data] × 4`，
   每条 8 字节 `[packed][value]`，`type = packed >> 20`、`variant = (packed >> 8) & 0xFF`；
   四个分段依次是「遇到 / 击杀 / 命中 / 死亡」。本插件**不在报告里展示**这一段（多一处未验证口径没意义），
   只在 `tools/build_boss_table.py` 里用它做上面 3、4 两条核对。
2. **实体名带不带 `#` 有讲究**：`name="#HUSH"` 这类是**语言包键**（可以查到官方简体译名），
   `name="Hush (Skinless)"` 这类是开发用字面名（查不到中文）。因此挑选「主实体」时
   优先选能查到官方中文名的那个——例如 `bossID 63` 的三个实体里只有 `#HUSH` 能查到「死寂」。
